# Shell scripts and Dockerfiles must use LF or Linux containers refuse to run them.
*.sh              text eol=lf
Dockerfile        text eol=lf
Dockerfile.*      text eol=lf
entrypoint.sh     text eol=lf
docker-compose*.yml text eol=lf
