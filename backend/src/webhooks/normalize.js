'use strict';
// Retained for backward compatibility. The real provider adapter now lives in
// src/providers/mantapay.js (createCheckout + parseWebhook).
module.exports = require('../providers/mantapay');
