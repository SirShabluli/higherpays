# Copy to `.env.local` (gitignored) and adjust for your setup.

# URL of the HigherPays API. Leave blank to hit the same origin as the
# frontend — useful when the frontend is served through a reverse proxy
# that already exposes /auth, /workspaces, etc.
VITE_API_URL=http://localhost:3000
